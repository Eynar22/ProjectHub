===============================================================================
ESTADO DE CUMPLIMIENTO DE LA CARPETA reglas/  —  ProjectHub (frontend)
===============================================================================
Última actualización: 2026-08-27
Rama de trabajo:       test   (todos los avances están commiteados aquí)
Último commit:         567e218  "moviendo login"  (rondas 1-5 ya commiteadas
  por el usuario, uno o varios commits por ronda — ver `git log` para el detalle).
Cambios sin commitear:  sí — sesión 2026-08-27, ronda 6: los 4 slices de
  Workspace/index.tsx -> react-query ya están HECHOS (chat, tareas,
  recursos+detalle de proyecto, solicitudes de unión) -esto cierra el follow-up
  de Anexo B que quedaba pendiente-. Slices 1 (CHAT) y 2 (TAREAS) ya fueron
  probados y confirmados por el usuario en navegador; en el camino aparecieron
  y se corrigieron 3 bugs reales (uno preexistente: el focus-trap de
  shared/components/ui/Modal.tsx robaba el foco en cada re-render ajeno, no
  solo al abrir) más dos rondas de trabajo en la fluidez del scroll del chat
  (mejoró bastante, el usuario dice que "aún no está del todo solucionado" —
  sigue abierto, ver FASE 2 sección CHAT punto 6). Slices 3 y 4 (RECURSOS+
  DETALLE DE PROYECTO, SOLICITUDES) se hicieron juntos al final y están
  PENDIENTES DE PROBAR en navegador — ver "PENDIENTE DE VERIFICAR EN
  NAVEGADOR" más abajo, en FASE 2. tsc/lint verdes (0 errores; 90 -> 25
  warnings acumulado en toda la sesión), build compila.

Objetivo global: aplicar los 3 documentos de reglas/ a todo el frontend
  - Manual-Reglas-UI-UX-Web-2026.txt
  - Anexo-A-Sistema-de-Color-UI.txt
  - Anexo-B-Arquitectura-Frontend-y-API.txt

Cómo verificar el estado en la PC nueva (desde PR-26-Busqueda-Proyectos-main/):
  npm install
  npx tsc --noEmit      -> debe dar 0 errores
  npm run lint          -> 0 errores, ~90 warnings (deuda de `any`, ver B7 abajo)
  npx vite build        -> debe compilar (queda 1 aviso de chunk >500kb, preexistente)
  npm run dev           -> revisar visualmente

NOTA: `npx vite build` NO hace typecheck. Siempre correr `npx tsc --noEmit` aparte.
El usuario hace los commits él mismo, slice por slice.


===============================================================================
FASE 1 — ANEXO A (SISTEMA DE COLOR)      ESTADO: COMPLETA
===============================================================================

HECHO:
  - src/styles/tokens.css  (NIVEL 1: primitivos)
      * Escala primaria OKLCH indigo, hue 262 (índigo real).
        (2026-08-27: se movió de 277° violeta -> 262°; A10 "Dashboard/Panel =
         azul o índigo" + A4 "azul = confianza/seguridad". Se regeneró escala
         indigo + grises tintados + sombras a 262°, mismo croma y luminosidad.)
      * Grises tintados con el tono del primario (croma 0.002-0.017).
      * Semánticos fijos (Anexo A9): verde/rojo/ámbar/azul con
        -subtle / -base / -strong / -on-dark. NO se eligen por gusto.
      * Espaciado base 4/8 y duraciones.
  - src/styles/theme.css  (NIVEL 2: semánticos + @theme)
      * Tokens semánticos que apuntan a los primitivos.
      * UN SOLO color de marca (indigo). `secondary` y `accent` son NEUTRALES,
        no colores. (decisión del usuario: "lo que dice Anexo A hazlo")
      * Modo oscuro TRADUCIDO (no invertido): .dark con neutros y marca
        re-mapeados; nunca #000 ni #fff puros.
      * @theme con radios, sombras (una sola fuente de luz, tintadas),
        z-index NOMBRADOS (--z-index-dropdown..toast, nunca 9999),
        curvas de easing.
      * Escala tipográfica fluida --text-xs..2xl con clamp() (ver Fase 3).
      * @layer base: prefers-reduced-motion, :focus-visible global,
        reglas h1-h4/label/button/input, scroll-margin-top.
  - index.html: <html lang="es"> + script anti-parpadeo de tema.
  - Cero HEX y cero utilidad de paleta cruda de Tailwind fuera de
    tokens.css / theme.css.
  - Tokens extra: surface-inverse (componentes siempre oscuros, p.ej. cards de
    Explore); paleta chart-1..5 para datos/categorías (no decoración).
  - Componentes propios alineados a Anexo A8 (jerarquía de botones) y 44px:
    Button, Card, Input/TextArea, Navbar, Sidebar.
  - SIN DEGRADADOS en todo src/ (Anexo A11 "✗ degradados"; Manual Parte 19):
      * `bg-gradient-to-* from-primary to-secondary` y variantes -> `bg-primary`.
      * placeholders `from-x/5 to-y/5` -> `bg-x/5` plano.
      * barras de cabecera `from-x/8 to-transparent` -> `bg-primary/5` o `bg-muted/50`.
      * texto con `bg-clip-text ... gradient` -> `text-primary` sólido.
      * scrim de foto de Explore -> `bg-black/50` plano.
      * conector del stepper de Home -> `h-px bg-border`.
      * Verificado: 0 `bg-gradient-to`, 0 `via-`, 0 `bg-clip-text` en src/.

FALTA (opcional / menor):
  - Manual Parte 5.2: `--text-3xl`+ siguen en el default de Tailwind (a propósito;
    solo se usan en héroes con modificadores responsivos md:/lg: explícitos).
    Si se quiere el rango completo del Manual (44-72px) habría que auditar y bajar
    los `text-3xl`/`text-4xl` sueltos de títulos de panel para que no se vuelvan
    gigantes.


===============================================================================
FASE 2 — ANEXO B (ARQUITECTURA FRONTEND Y API)   ESTADO: ESENCIALMENTE COMPLETA
===============================================================================

Estructura final (Anexo B2):
  src/
    App.tsx (fino), main.tsx
    app/         context/ (AppContext puente), guards/ (RutaPrivada, RutaPorRol),
                 providers.tsx, router.tsx, TitleSync.tsx
    pages/       (+ Workspace/)  <- todas las páginas
    features/    auth/ empresas/ proyectos/ usuarios/ workspace/
                 cada una: services/ hooks/ types/ index.ts (barrel)
    shared/      components/{ui,layout,feedback}, types/, utils/
    lib/         api/{client,endpoints,errors}, config.ts, storage.ts, queryClient.ts
    styles/      tokens.css, theme.css

HECHO:
  Stage A — Infra:
    - .editorconfig, .prettierrc, .env.example.
    - lib/config.ts, lib/storage.ts.
    - lib/api/client.ts: UN cliente HTTP fetch-based, timeout 15s, 401 -> /login,
      clase ApiError. lib/api/endpoints.ts: TODAS las rutas del backend
      centralizadas (ENDPOINTS.*). lib/api/errors.ts.
    - tsconfig.json reparado para Vite: module ESNext, moduleResolution bundler,
      baseUrl + paths @/*, allowImportingTsExtensions, include ["src"].
    - Tipos sacados del god-context a shared/types/ + features/*/types/.

  Stage B — Slice auth:
    - @tanstack/react-query v5 + lib/queryClient.ts + QueryClientProvider.
    - features/auth/: auth.service.ts (login, obtenerPerfil, registrarEmpresa/
      Empleado con File->base64 interno, recuperación, cambiarPassword),
      hooks/useAuth.ts (useMutation por operación), barrel.
    - shared/utils/fileToBase64.ts.
    - Páginas migradas a hooks: ForgotPassword, ResetPassword, ForceChangePassword,
      Register (no importan `api`).

  Stage C — Slices de dominio (proyectos, empresas, usuarios, workspace):
    - features/proyectos/: services (proyectos + solicitudes de participación),
      hooks (useProyectos, useProyecto, useCrearProyecto, useCambiarEstadoProyecto,
      useAutoTerminarProyectos, useTransferirProyecto; refetchInterval 30s que se
      salta si la pestaña está oculta), barrel.
    - features/empresas/: services (listar, obtenerPorId, actualizar, aprobar,
      bloquear, desbloquear, eliminar), hooks (useEmpresas, useEmpresa,
      useActualizarEmpresa, useModerarEmpresa), barrel.
    - features/usuarios/: services (listar, actualizarPerfil, moderar,
      solicitudes de membresía), hooks (useUsuarios, useUsuario, useModerarUsuario,
      useActualizarPerfil, useSolicitudesMembresia, ...), barrel.
    - features/workspace/: services {tareas, chat, recursos} (recursos con
      subirArchivo FormData), hooks {useTareas, useChat (refetch 3s), useRecursos,
      useSubirArchivo}, barrel.
    - Toasts movidos a los hooks (onSuccess/onError).

  Stage D — Mover carpetas (git mv):
    - pages -> src/pages ; componentes propios -> shared/components/{ui,layout,feedback} ;
      fileUtils -> shared/utils ; routes.tsx -> app/router.tsx ; App.tsx fino ;
      provider tree -> app/providers.tsx ; guards -> app/guards/.
    - BORRADOS como código muerto (0 importadores): 48 archivos shadcn de
      components/ui/*, components/figma/ImageWithFallback, components/ProtectedRoute.
    - Todos los imports a alias absoluto `@/...` (cero `../../..`).

  Stage E — ESLint + limpieza:
    - eslint@9 + typescript-eslint + eslint-plugin-react-hooks + react-refresh +
      prettier. eslint.config.mjs (flat config).
    - Scripts en package.json: lint, lint:fix, typecheck, format, format:check, preview.
    - README reescrito al mínimo B15 (variables de entorno front+back, estructura, comandos).

  God-context (AppContext.tsx):
    - Reducido de 771 -> 291 líneas. Ya NO importa `api`, NO tiene estado de
      dominio propio, NO tiene polling ni loadInitialData.
    - Es un PUENTE: expone projects/companies/users/requests desde los hooks de
      react-query + wrappers de mutación que delegan en los services + un useEffect
      de persistencia de sesión (token -> authService.obtenerPerfil).
    - `logout` hace queryClient.clear().
    - src/app/services/api.ts ELIMINADO. Cliente único en lib/api/client.
    - (2026-08-27) Bloque de re-export de tipos ELIMINADO: los 8 imports que
      quedaban (`import type {...} from '@/app/context/AppContext'` en
      CompanyDashboard, CompanyMembers y los Workspace/*Tab.tsx) se movieron a
      los barriles de cada feature (@/features/empresas, /proyectos, /usuarios).
      AppContext.tsx ahora solo importa esos tipos para sus propias firmas
      internas, ya no los re-exporta.

  Decisión (2026-08-27): NO se separa el repo en frontend/ + backend/. El usuario
  eligió dejarlo así; la estructura interna del Anexo B ya está y el layout actual
  (app en la raíz + backend/ como subcarpeta) es estándar y no rompe ninguna regla.

FALTA (follow-ups, funciona todo pero no es 100% Anexo B):
  - [HECHO 2026-08-27 — LOS 4 SLICES] Workspace/index.tsx: pasado de estado
    local + polling/fetch manual a react-query (hooks de features/workspace/
    y features/proyectos/). Se hizo en slices chicos, cada uno verificado en
    navegador por el usuario antes de seguir con el siguiente (acordado
    2026-08-27). Slices 1 y 2 (CHAT, TAREAS) quedaron confirmados por el
    usuario en navegador antes de seguir; slices 3 y 4 (RECURSOS+DETALLE DE
    PROYECTO, SOLICITUDES DE UNIÓN) se hicieron juntos al final -están
    entrelazados en el código- y quedan PENDIENTES DE PROBAR (ver la lista al
    final de esta sección). Con esto, Workspace/index.tsx ya no tiene ningún
    `useState` haciendo de caché de datos del servidor ni ningún fetch manual
    en un useEffect -todo pasa por hooks de react-query-, cumpliendo el
    objetivo original del follow-up. Warnings de lint acumulados en esta
    migración: 90 -> 25 (bajaron con cada slice, sobre todo por sacar
    useEffects con setState directo, que gatillaban un warning cada uno).
    [HECHO Y CONFIRMADO 2026-08-27, slice 1/4 — CHAT] `chatMessages`/`sendingMsg`/
      el polling manual con setInterval de 3s se reemplazaron por
      `useMensajesChat(project?.id)` (mismo intervalo de 3s, pero ahora se PAUSA
      si la pestaña está oculta — antes seguía pegándole al backend en segundo
      plano) y `useEnviarMensaje(project?.id ?? '')`. Se perdió el update
      optimista (el mensaje ya no aparece instantáneo, espera el round-trip).
      Usuario probó en navegador el 2026-08-27: "se ve bien, está funcionando".
      Confirmado, no se toca más.
    [HECHO 2026-08-27, slice 2/4 — TAREAS (kanban)] `kanbanColumns`/`localTasks`/
      `loadingTasks` (estado local + fetch manual en un useEffect) reemplazados
      por `useColumnasProyecto(project?.id)` + `useTareasProyecto(project?.id)`
      (loadingTasks = OR de ambos `isLoading`). Los handlers (crear/mover/editar/
      comentar/eliminar tarea) pasaron a usar `useCrearTarea` / `useActualizarTarea`
      / `useEliminarTarea` / `useAgregarComentario` (proyectoId fijado al crear
      cada hook, mutationFn se recrea con el proyecto actual en cada render — no
      hay problema de closure viejo porque el usuario no puede accionar antes de
      que el componente re-renderice con el project.id real).
      Se ensanchó el tipo de `useActualizarTarea` en
      features/workspace/hooks/useTareas.ts (`datos: Partial<Task>` ->
      `Partial<Task> & Record<string, unknown>`) porque la firma original no
      dejaba pasar `usuario_ids` (campo de escritura de la relación ManyToMany,
      no existe en el modelo de lectura `Task`) ni `fecha_limite: null` (para
      borrar la fecha límite). Cambio seguro: este hook no lo usaba nadie más
      todavía (era el primer consumidor real).
      DECISIÓN DE DISEÑO — drag&drop SÍ lleva update optimista (a diferencia del
      chat): `handleMoveTask` escribe directo en la caché de react-query con
      `queryClient.setQueryData(TAREAS_KEYS.lista(project.id), ...)` ANTES de
      llamar a la mutación, así la tarjeta se ve en la columna nueva al soltarla,
      no después de que vuelva el request. Si no fuera así, soltar una tarjeta
      se vería "roto" (vuelve a su lugar y salta a la posición correcta después),
      mucho más notorio que el mensaje de chat con un poco de delay.
      El comentario nuevo en el modal de tarea SÍ sigue siendo instantáneo (se
      parchea `editingTask` local con la respuesta del POST, igual que antes —
      no dependía del polling).
      `ensureColumns` (crea las 3 columnas por defecto la primera vez que se usa
      el tablero) se mantiene con `tareasService.crearColumna` directo (no hay
      hook dedicado, es un bootstrap raro) + invalidar la query de columnas al
      final. tsc/lint verdes (0 errores; 30 -> 29 warnings).
      Quedan 2 slices: RECURSOS+DETALLE DE PROYECTO (projectCompleto/
      projectMembers), SOLICITUDES DE UNIÓN.

    [HECHO 2026-08-27, 3 BUGS reportados por el usuario al probar el slice
      TAREAS — corregidos]
      1. BUG PREEXISTENTE, NO introducido por esta migración, pero encontrado
         de paso: shared/components/ui/Modal.tsx (el modal accesible con
         focus-trap, usado por TODOS los modales de la app) tenía
         `useEffect(..., [open, onClose])`. Como casi todos los que usan
         <Modal> le pasan `onClose` como arrow function inline (identidad
         nueva en cada render del padre), el efecto se re-ejecutaba en CADA
         re-render del padre — no solo al abrir el modal — y esa re-ejecución
         vuelve a robar el foco hacia el primer elemento interactivo del panel.
         Síntoma exacto que reportó el usuario: escribir una sola letra en el
         comentario de una tarea sacaba el foco del textarea y lo mandaba al
         botón "Cerrar" (X) del modal — porque en el DOM ese botón aparece
         antes que el textarea, así que es el "primer elemento interactivo".
         Esto ya pasaba antes de tocar Workspace/index.tsx (el estado del
         comentario siempre vivió arriba, cualquier tecleo ya re-renderizaba
         todo el árbol) pero probablemente nunca se notó tan claro como ahora.
         Arreglo: el efecto ahora depende solo de `[open]`; `onClose` se guarda
         en un ref que se actualiza en un efecto aparte (sin condición de
         carrera, patrón estándar "latest ref"), así el ESC sigue llamando a
         la versión más reciente sin forzar que el efecto principal se
         re-dispare en cada tecleo. Beneficia a TODOS los modales de la app,
         no solo al de tareas.
      2. features/workspace/components/ChatTab.tsx: el input de texto tenía
         `disabled={sending || isReadOnly}`. Un <input> deshabilitado pierde el
         foco automáticamente (comportamiento del navegador, no de React) —
         por eso al mandar un mensaje el campo "se deseleccionaba" y había que
         volver a clickearlo. Se sacó `sending` de esa condición: ahora solo el
         botón de enviar se deshabilita mientras la mutación está en curso, el
         campo de texto se puede seguir usando sin perder el foco.
      3. Reportado: los cambios (mover tarjeta de columna, agregar comentario)
         no se reflejaban en la vista de OTRO usuario sin recargar la pestaña.
         Causa: `useColumnasProyecto`/`useTareasProyecto` no tenían
         `refetchInterval` (a diferencia del chat, que sí polleaba cada 3s) —
         esto YA pasaba antes de esta sesión (el código viejo tampoco tenía
         ningún polling de tareas), no es una regresión de esta migración, pero
         era la oportunidad obvia de arreglarlo ahora que ya está en
         react-query. Se agregó el mismo patrón que usan el resto de los hooks
         del proyecto (`refetchInterval: () => document.hidden ? false : 10_000`,
         se pausa con la pestaña oculta). Con el fix del Modal ya hecho, este
         polling no debería volver a robar el foco del modal de edición de
         tarea (antes de ese fix, agregar polling acá habría empeorado el bug 1).
      tsc/lint verdes (0 errores, 29 warnings, igual que el slice anterior),
      build compila.
      4. [HECHO 2026-08-27, con más detalle del usuario] "la barra de chat al
         subir/bajar no está fluida... cuando scrolleo y me detengo, después
         de un segundo salta directo al lugar" / "todo el chat se mueve junto
         con lo que estoy scrolleando". Causa: el auto-scroll-al-final
         (`scrollIntoView({behavior:'smooth'})`) se disparaba cada vez que
         cambiaba la cantidad de mensajes SIN importar si el usuario estaba
         scrolleado hacia arriba leyendo el historial — con el chat consultando
         cada 3s, cualquier mensaje nuevo (o incluso una cuenta que fluctúe)
         lo secuestraba de vuelta al final, interrumpiendo la lectura. Este
         efecto YA estaba en el código original (no es una regresión de esta
         sesión), pero antes vivía en Workspace/index.tsx sin forma de saber
         si el usuario estaba lejos del final.
         Arreglo: el auto-scroll se movió adentro de
         features/workspace/components/ChatTab.tsx (que sí tiene acceso al
         contenedor real con overflow-y-auto). Ahora escucha el scroll del
         contenedor de mensajes y guarda en un ref si el usuario está a menos
         de 80px del final. El efecto que dispara `scrollIntoView` solo actúa
         si `cercaDelFinalRef.current` es true — si el usuario scrolleó arriba
         a propósito, un mensaje nuevo ya no lo saca de ahí. Al entrar al chat
         arranca en true (muestra los últimos mensajes, como siempre).
         Efecto secundario de la refactorización: `messagesEndRef` ya no se
         crea/pasa desde Workspace/index.tsx (era una prop que solo usaba
         ChatTab) — se volvió estado interno de ChatTab.
         tsc/lint/build verdes (0 errores, 29 warnings, sin cambio).
      5. [HECHO 2026-08-27] El usuario aclaró que lo anterior no era todo:
         además del salto al final, el scroll EN SÍ se sentía trabado mientras
         lo hacía activamente ("como pausado, de golpe se mueve cuando te
         detienes") — y confirmó que pasa SOLO en el chat, en el resto de la
         app el scroll es fluido (dato clave: descarta que sea el fondo
         animado global de esferas borrosas, que corre en todas las páginas).
         Causa: el listener `onScroll` que se acababa de agregar (punto 4)
         lee `scrollHeight`/`scrollTop`/`clientHeight` en cada evento de
         scroll, y con mouse/trackpad de alta precisión ese evento puede
         disparar decenas de veces por segundo — cada lectura de esas tres
         propiedades fuerza al navegador a recalcular el layout de forma
         sincrónica en pleno gesto de scroll (layout thrashing), lo cual se
         siente exactamente como "trabado, salta cuando parás". Arreglo:
         agrupar esa lectura a máximo una vez por frame con
         `requestAnimationFrame` (con cleanup de `cancelAnimationFrame` al
         desmontar) en vez de en cada evento nativo. Patrón estándar para
         listeners de scroll, sin cambiar el comportamiento (mismo resultado,
         como mucho un frame de latencia en detectar "cerca del final").
         tsc/lint/build verdes (0 errores, 29 warnings, sin cambio).
      6. [HECHO 2026-08-27, MEJORA PARCIAL — usuario: "aún no está del todo
         solucionado"] Intento adicional tras el punto 5 (usuario: "el chat
         aún sigue un poco lento"). Causa adicional: Workspace/index.tsx
         re-renderiza cada vez que el chat (3s) O las tareas (10s) consultan
         en segundo plano, sin importar qué pestaña esté mirando el usuario
         -ambos hooks se llaman siempre, no solo cuando su pestaña está
         activa-. ChatTab no estaba memoizado, así que cualquiera de esos
         polls (aunque no tuviera nada que ver con el chat) lo volvía a
         dibujar entero -incluyendo re-armar la lista completa de mensajes-,
         lo cual se siente como una traba si coincide con que el usuario está
         scrolleando. Arreglo: `ChatTab` envuelto en `React.memo` (se saltea
         el re-render si ninguna de sus props cambió). Para que el memo
         funcione de verdad, `handleSendMessage` en Workspace/index.tsx pasó
         a `useCallback` desestructurando `mutateAsync`/`isPending` del
         objeto `enviarMensaje` en vez de depender del objeto completo (los
         métodos mutate de react-query son estables entre renders; el objeto
         contenedor no necesariamente). Quedó movido antes de los `if (!project)
         return` / `if (!isParticipant) return` -junto a los demás hooks del
         chat- porque un Hook no puede llamarse después de un return
         condicional (regla de los Hooks; el primer intento rompía el build).
         tsc/lint/build verdes (0 errores, 29 warnings, sin cambio).
         [CONFIRMADO 2026-08-27] El usuario probó de nuevo más tarde y avisó
         que el scroll del chat "ahora está funcionando bien". Cerrado —
         entre el memo de ChatTab (punto 6) y el throttling con
         requestAnimationFrame (punto 5) quedó resuelto del todo.

    [HECHO 2026-08-27, slices 3/4 y 4/4 — RECURSOS+DETALLE DE PROYECTO y
      SOLICITUDES DE UNIÓN, hechos juntos porque están entrelazados en el
      código original]
      - `projectCompleto` (useState<Project|null> + fetch manual en
        `cargarProyectoDetalle`, llamado desde un useEffect en [id]) ->
        `useProyecto(id)` (ya existía, la usa también ProjectDetail.tsx).
      - `projectMembers` (useState<WorkspaceMember[]>, se llenaba dentro de
        `cargarProyectoDetalle` deduplicando creador+participantes) -> pasó a
        ser un valor derivado con `useMemo(() => {...}, [project, creador])`
        en vez de estado propio -ya no hace falta un fetch aparte, esos datos
        ya vienen dentro de lo que trae `useProyecto`-. Misma lógica de
        deduplicación y el mismo fallback defensivo `p.usuario ?? p`,
        palabra por palabra, solo que ahora recalculado en vez de fetcheado.
      - Los 3 lugares que llamaban a `cargarProyectoDetalle()` a mano después
        de crear/subir/borrar un recurso (`handleAddFolder`,
        `handleFileSelected`, `handleDeleteResource`) se sacaron: los hooks
        `useCrearRecurso`/`useEliminarRecurso` (features/workspace) YA
        invalidaban `PROYECTOS_KEYS.todos` en su `onSuccess` desde antes de
        esta sesión -la re-carga manual era redundante, react-query ya lo
        hacía solo-.
      - `handleToggleAccesoTareas` (dar/quitar acceso para crear tareas) y
        `handleSaveProjectInfo` (editar descripción/fecha fin/fotos) hacían
        `setProjectCompleto(...)` para reflejar el cambio al instante sin
        esperar a un refetch. Como `projectCompleto` ya no es estado propio,
        se cambió a `queryClient.setQueryData(PROYECTOS_KEYS.detalle(project.id), ...)`
        -mismo resultado (parche instantáneo de la caché), ahora escribiendo
        directo en la caché de react-query en vez de en un useState-. No hay
        hook dedicado para `cambiarRolParticipante`/`actualizar` de proyecto
        todavía, así que se siguen llamando esos dos métodos de
        `proyectosService` directo (como antes), solo cambió cómo se refleja
        el resultado en la UI.
      - `projectJoinRequests`/`loadingRequests` (useState + fetch manual en
        un useEffect que solo corría si `currentUser.id === project.creador_id`)
        -> `useSolicitudesDeProyecto(esDueno ? project?.id : undefined)` (ya
        existía, la usa también ProjectRequests.tsx desde una sesión
        anterior). Pasar `undefined` cuando no es el dueño desactiva el
        fetch vía el `enabled` interno del hook -mismo efecto que el `if`
        que cortaba antes-, sin violar la regla de los Hooks (no se puede
        NO llamar al hook condicionalmente, pero sí controlar qué le llega).
      - `handleAcceptJoinRequest`/`handleRejectJoinRequest` pasaron de
        `solicitudesService.aceptar/rechazar` + parche manual de estado a
        `useResponderSolicitud().mutateAsync({solicitudId, accion})` (ya
        existía, mismo hook usado en ProjectRequests.tsx) — invalida solo,
        y de paso ahora muestra un toast de confirmación (el hook lo trae
        incorporado; antes el código de Workspace no mostraba ninguno).
      - Import de `ProyectoSolicitud` (tipo que ya no se usaba directo en
        este archivo) sacado; `solicitudesService` sacado del import de
        features/proyectos (ya no se llama directo, todo pasa por los hooks).
      tsc/lint/build verdes (0 errores; 26 -> 25 warnings).

    [HECHO 2026-08-27] Bugs encontrados por el usuario al probar los slices
      3/4: ver detalle abajo (uno corregido ya, el otro en investigación).
      1. [HECHO] features/workspace/components/ResourcesTab.tsx usaba el
         `confirm()` nativo del navegador para borrar un recurso (carpeta o
         archivo/imagen) — el típico cartel "localhost:5173 dice: ..." que no
         tiene nada que ver con el diseño de la app. Se cambió por el mismo
         patrón de confirmación que ya usa AdminUsers.tsx (Modal propio con
         ícono de alerta, "Cancelar"/"Eliminar", el texto avisa si es una
         carpeta que "se eliminará también todo su contenido"). Quedan sin
         tocar otros 2 `confirm()` nativos preexistentes en el resto de la
         app (AdminCompanyRequest.tsx, AdminCompanyUsers.tsx) — no eran parte
         de lo reportado ni de lo que se estaba probando esta sesión, mismo
         defecto pero fuera de alcance por ahora.
         tsc/lint/build verdes (0 errores, 25 warnings, sin cambio).
      2. [HECHO 2026-08-27 — BUG DE BACKEND, no de frontend] La hora que se
         muestra en los mensajes del chat y en los comentarios de tareas no
         coincidía con la hora actual. Se investigó backend/ (solo lectura)
         antes de tocar nada, para no parchear a ciegas del lado equivocado.
         Diagnóstico confirmado: `mensaje.fecha` y `tarea_comentario.fecha_creacion`
         son columnas Postgres `timestamp WITHOUT time zone`
         (backend/src/entities/mensaje.entity.ts, tarea-comentario.entity.ts).
         Postgres corre en un contenedor Docker sin TZ configurado -> UTC. El
         backend en desarrollo corre suelto en la máquina (`npm run start:dev`,
         no en su contenedor) con la hora local de la máquina (Bolivia, UTC-4).
         El driver `pg` interpreta esos timestamps "sin zona" usando la zona
         horaria del PROCESO DE NODE al leerlos -no la de la base-, así que
         una hora guardada como 18:30 (UTC, correcta) se leía como si ya fuera
         18:30 hora local, corriendo el reloj mostrado 4h para adelante.
         Importante: la escritura SIEMPRE fue correcta (las columnas usan
         `default: () => 'CURRENT_TIMESTAMP'`, generado por Postgres mismo,
         Node nunca participa al guardar) — el problema era solo de LECTURA,
         así que el fix corrige los mensajes viejos también, no hace falta
         migrar datos. Tampoco es un bug que fuera a aparecer en producción
         si el backend corre dentro de su contenedor Docker (ahí Postgres y
         Node están en la misma zona, UTC) — es específico de correr el
         backend suelto en Windows en desarrollo.
         Fix aplicado: `TZ=UTC` agregado a backend/.env (para `npm run
         start:dev`) y al `environment:` del servicio `backend` en
         docker-compose.yml (explícito, aunque la imagen node:20-alpine ya
         usa UTC por defecto). Node 20 relee `process.env.TZ` en cada
         operación de fecha -no la cachea solo al arrancar-, así que alcanza
         con esto, no hace falta tocar código. REQUIERE reiniciar el proceso
         del backend a mano (parar y volver a correr `npm run start:dev`) —
         al ser una variable de entorno y no un archivo de código, el modo
         watch de NestJS no lo recarga solo.
         No se tocó nada del frontend para esto (ChatTab.tsx/TaskEditModal.tsx
         ya estaban bien, el `new Date(...).toLocaleTimeString(...)` es
         correcto una vez que el backend manda la fecha bien).
      3. [HECHO 2026-08-27] Reportado por el usuario en AdminCompanyRequest.tsx:
         un ícono y el texto "Ver Documento" invisibles (mismo color que el
         fondo). Causa raíz — bug de Anexo A/Fase 1, NO de esta sesión de
         react-query: `secondary` y `accent` se redefinieron como NEUTRALES
         (ambos = `gray-100`, un gris casi blanco) cuando se aplicó la regla
         "un solo color de marca" del Anexo A (ver FASE 1 más arriba). Varios
         archivos seguían usando `text-secondary`/`bg-secondary`/`text-accent`/
         `bg-accent` como si fueran colores vistosos de acento (como eran
         ANTES de esa regla) en vez de como superficies neutras — ese barrido
         nunca se hizo completo en su momento. Resultado: texto/íconos gris
         casi blanco sobre fondos igual de claros (`bg-card`, `bg-muted`) ->
         invisibles o casi.
         Se buscó en TODO src/ (no solo en el archivo reportado) y se
         encontraron 7 archivos con el mismo defecto, corregidos todos:
           AdminCompanyRequest.tsx — ícono+franja+badge PDF+texto "Ver
             Documento" de la sección Documentación (6 usos) -> `primary`.
           CompanyProfile.tsx — ícono de sección Empresa, insignia de cámara
             de foto (fondo sólido `bg-secondary` con texto blanco encima,
             doblemente invisible), tarjeta+botones Ver/Descargar del
             documento de empresa (7 usos) -> `primary`.
           CompanyDashboard.tsx / CompanyMembers.tsx — botón "Ver Documento"
             de solicitudes de membresía, que además tenía `hover:text-primary`
             -o sea que SOLO se veía bien al pasar el mouse-; se sacó ese
             hover de color y se cambió por `hover:underline` (patrón ya usado
             en otros links de la app, p. ej. Register.tsx).
           CreateProject.tsx — dos íconos de encabezado de sección (antes
             secondary/accent, ahora `success` para Fechas y Financiamiento,
             `info` para Imágenes, así las 4 secciones del formulario quedan
             con colores distintos entre sí: primary/success/info/warning).
           AdminProjects.tsx / features/workspace/components/InfoTab.tsx —
             ícono de "colaboradores"/participantes -> `primary` (mismo
             color que ya usa el ícono equivalente en MyProjects.tsx).
           features/workspace/components/ResourcesTab.tsx — insignia "IMG"
             de miniaturas de imagen (antes `accent`) -> `info`, así queda
             distinta de la insignia "PDF" (`destructive`) y de las carpetas
             (`primary`).
           Home.tsx — blob decorativo de fondo del hero e ícono de
             "Ubicación" en el buscador -> `primary` (el blob era solo
             estético, no un bug de legibilidad, pero se alineó de paso).
         Ningún color nuevo inventado: solo se usaron `primary` o los
         semánticos ya sancionados por el Anexo A9 (`success`/`info`/`warning`/
         `destructive`), que ya se usaban en el resto de la app. Verificado
         con una búsqueda final: 0 usos de `text-secondary`/`bg-secondary`/
         `text-accent`/`bg-accent` (fuera de `-foreground`) en todo src/.
         tsc/lint verdes (0 errores, 25 warnings, sin cambio), build compila
         (CSS bajó ~1.3kB al sacar variantes de color que ya no se usaban).
         PENDIENTE DE VERIFICAR EN NAVEGADOR: revisar visualmente los 7
         archivos tocados (AdminCompanyRequest, CompanyProfile, CompanyDashboard,
         CompanyMembers, CreateProject, AdminProjects, Home + InfoTab/ResourcesTab
         de Workspace) en modo claro Y oscuro, para confirmar que los nuevos
         colores se vean bien y no quede ningún otro lugar con el mismo patrón
         que se haya escapado de la búsqueda por texto.
      4. [HECHO 2026-08-27] Reportado por el usuario en Login.tsx (ahora
         features/auth/components/LoginForm.tsx): en el osito animado, los
         ojos y el hocico (blancos, sólidos) se perdían "en fondo blanco", y
         el aro/marco que rodea al osito no correspondía a la paleta y
         pidió que fuera sólido. Causa: este SVG dibujado a mano nunca se
         tocó en el barrido de "sin degradados" del Anexo A11 (Slice 8, FASE
         1) porque son `<linearGradient>`/`<radialGradient>` de SVG puro, no
         clases `bg-gradient-to-*` de Tailwind -la búsqueda de texto de aquel
         momento no los iba a encontrar-. Tenía 3 degradados:
           `bearGradient` (pelaje): primary -> secondary. Como `secondary` es
             gris casi blanco (mismo tema del punto 3 de más arriba), la
             zona del degradado que cae cerca de los ojos/hocico (blancos,
             sólidos) queda igual de clara que ellos -> blanco sobre casi-
             blanco, invisible. Esta es la causa real de lo reportado.
           `ringGradient` (marco exterior): destructive -> secondary ->
             `color-mix(secondary, black)` -> mezcla roja+gris casi
             blanco+negro puro mezclado, que no es una combinación
             reconocible de la paleta (y usa negro puro, contra la regla de
             Anexo A de nunca usar #000/#fff puros).
           `innerWindow` (ventana interior, radial): card -> muted -> border,
             un degradado neutro sutil (efecto túnel).
         Arreglo: los 3 degradados se sacaron de `<defs>` y sus 6 usos (orejas,
         cabeza, marco, ventana, + los 2 brazos que también usaban
         `bearGradient`) pasaron a color sólido: pelaje/orejas/brazos/marco ->
         `fill-primary` (mismo color de marca en todo el osito, incluido el
         marco -antes con 3 colores ajenos entre sí-), ventana interior ->
         `fill-muted` (plano, ya no degradado). La sombra del marco
         (`dropShadow` filter) usaba `floodColor: var(--secondary)` -un
         "tinte" casi invisible-, se cambió a `var(--primary)` para que la
         sombra se note y sea del color de marca (mismo criterio que las
         sombras tintadas de theme.css). Con el pelaje ahora sólido indigo en
         vez de degradado hacia blanco, ojos y hocico (blancos) contrastan
         bien contra el fondo.
         tsc/lint/build verdes (0 errores, 25 warnings, sin cambio).
         [CONFIRMADO 2026-08-27] El usuario probó en modo oscuro (mandó
         captura): marco/pelaje/ojos/hocico ya se ven bien. De paso notó que
         nariz/boca/pupilas (usaban `text-foreground`/`fill-foreground`, que
         SÍ sigue el tema) se veían grisáceas/apagadas en modo oscuro -foreground
         se aclara ahí para servir de texto legible-, mientras que ojos/hocico
         (blanco fijo) se ven parejos siempre. Pidió que nariz/boca/pupilas
         también queden fijas, iguales en ambos modos: se cambiaron de
         `foreground` (token, sigue el tema) a `black` literal (fijo, mismo
         criterio que ya se usa para el blanco de ojos/hocico) — la cara del
         osito completa (blanco de ojos/hocico + negro de nariz/boca/pupilas)
         ahora es igual en claro y en oscuro, no se mezcla con el tema.
         tsc/lint/build verdes (0 errores, 25 warnings, sin cambio).

PENDIENTE DE VERIFICAR EN NAVEGADOR (sesión 2026-08-27, slice TAREAS + 3 bugs
corregidos — re-test después de los fixes de Modal/ChatTab/polling):
  1. npm run dev, entrar a un proyecto -> pestaña Tareas.
  2. Si el proyecto no tiene tablero todavía: crear la primera tarea y
     confirmar que arma las 3 columnas por defecto (Por Hacer/En Proceso/
     Completado) y la tarea cae en "Por Hacer".
  3. Arrastrar una tarjeta a otra columna: tiene que verse instantáneo al
     soltarla.
  4. Abrir una tarea (click en la tarjeta) y en el campo de comentario escribir
     una frase completa SIN que el foco salte a ningún lado (antes se rompía
     con la primera letra) -> agregar el comentario, tiene que aparecer al
     toque.
  5. Editar título/descripción/prioridad/fecha/asignados y guardar -> confirmar
     que el cambio se refleja en la tarjeta del tablero.
  6. Eliminar una tarea desde el modal -> confirmar que desaparece del tablero.
  7. Sincronía entre usuarios: con dos sesiones (dos navegadores o uno en
     incógnito) en el mismo proyecto, mover una tarjeta o agregar un
     comentario en una, y confirmar que en la OTRA aparece solo en unos ~10s
     sin recargar la página.
  8. Chat: mandar un mensaje y confirmar que el campo de texto NO pierde el
     foco (antes había que volver a clickearlo después de cada envío).
  9. Abrir CUALQUIER OTRO modal de la app (ej. confirmar bloqueo de usuario en
     Admin > Usuarios) y escribir/interactuar adentro, para confirmar que el
     fix de Modal.tsx no rompió nada ahí (ese componente lo usan todos los
     modales, no solo el de tareas).
  10. Seguí atento a si la barra de scroll del chat se siente lenta/no fluida
      — contame en qué momento exacto pasa (¿al scrollear vos con el mouse
      hacia arriba en el historial, o es el "salto" automático hacia abajo
      cuando llega un mensaje nuevo?) para poder buscarlo con más precisión.
  11. [Ya se siguió con RECURSOS+DETALLE DE PROYECTO y SOLICITUDES sin esperar
      confirmación explícita de este punto 11 en particular -el usuario dio
      el visto bueno general de tareas y pidió continuar-; si algo de la lista
      de arriba (1-10) todavía no se probó, sigue pendiente igual.]

PENDIENTE DE VERIFICAR EN NAVEGADOR (sesión 2026-08-27, slices 3/4 y 4/4 —
RECURSOS+DETALLE DE PROYECTO y SOLICITUDES DE UNIÓN):
  1. npm run dev, entrar a un proyecto -> pestaña Información: confirmar que
     carga bien nombre/descripción/fechas/fotos (viene de `useProyecto`, no
     de un fetch manual).
  2. Si sos Administrador de Empresa con acceso al proyecto: editar
     descripción/fecha fin/fotos desde esa pestaña y guardar -> confirmar que
     se ve reflejado al instante (no debería tardar ni recargar).
  3. Pestaña Recursos: crear una carpeta nueva -> confirmar que aparece sin
     recargar la página.
  4. Subir un archivo -> confirmar que aparece en la carpeta actual sin
     recargar.
  5. Eliminar un recurso (archivo o carpeta) -> confirmar que desaparece sin
     recargar.
  6. Pestaña Equipo: si sos el dueño del proyecto, dar/quitar acceso para
     crear tareas a un colaborador -> confirmar que el cambio de rol se ve
     al instante en esa fila.
  7. Pestaña Solicitudes (solo visible si sos el dueño del proyecto): pedirle
     a otro usuario que mande una solicitud de unión al proyecto (o generarla
     de prueba) y confirmar que aparece en la lista de pendientes.
  8. Aceptar o rechazar una solicitud desde ahí -> confirmar que se mueve a
     "procesadas" y que sale un toast de confirmación (esto es nuevo: antes
     no mostraba ningún toast al aceptar/rechazar desde el Workspace).
  9. Si NO sos el dueño del proyecto: confirmar que la pestaña "Solicitudes"
     ni siquiera aparece en la lista de pestañas (verifica que el `enabled`
     condicional del hook sigue cortando el fetch para no-dueños).
  10. Si algo de esto se ve mal, avisar. Si todo sale bien, con esto se cierra
      por completo la migración de Workspace/index.tsx a react-query — lo
      único que queda pendiente en todo el proyecto es lo que ya requiere
      herramientas de navegador (contraste, zoom 200%) y el scroll del chat
      "un poco lento" que no se terminó de resolver (punto 6 de la sección
      CHAT más arriba).
  - [HECHO 2026-08-27] Páginas que leían el PUENTE del context
    (useApp().projects / companies / users / requests) migradas a los hooks
    directos de cada feature: AdminDashboard, Explore, ProjectRequests, Sidebar,
    ProjectDetail, AdminProjects, MyProjects (subcomponente ProjectCard).
    Mismo query key -> react-query comparte la caché, cero llamadas de red de
    más. `currentUser`/`openBase64` siguen viniendo de useApp() (son estado de
    auth, no server-state). tsc/lint/build verdes, 90 warnings (igual que antes).
    En ProjectRequests.tsx además se cambió `updateRequest` (función del puente)
    por el hook `useResponderSolicitud` — mismo resultado pero ahora sí muestra
    un toast de confirmación al aceptar/rechazar (antes no mostraba ninguno).
  - [HECHO 2026-08-27] Bug corregido en ProjectRequests.tsx (ruta
    /project/:id/requests): usaba `useSolicitudesEnviadas` ("solicitudes que YO
    envié a proyectos ajenos") para listar las solicitudes de un proyecto del
    que el usuario actual es dueño. Como esta página solo se muestra cuando
    `project.creador_id === currentUser?.id`, esa fuente de datos prácticamente
    nunca podía traer nada (uno no se envía solicitudes a sí mismo) — la página
    mostraba "0 pendientes" siempre, sin importar cuánta gente pidiera unirse de
    verdad. Cambiado a `useSolicitudesDeProyecto(id)` ("recibidas en un proyecto
    propio"), que ya existía en features/proyectos y es lo que usa correctamente
    Workspace/SolicitudesTab. Se eliminó también el filtro manual
    `.filter(r => r.proyecto_id === Number(id))` que ya no hace falta porque el
    hook nuevo escopa por proyecto en el propio query. tsc/lint/build verdes.
  - Mover UI de features fuera de pages/:
      [HECHO 2026-08-27] Workspace/*.tsx (los 6 *Tab.tsx + AssigneeSelector,
        NewFolderModal, TaskCard, TaskColumn, TaskEditModal, types.ts) movidos
        con `git mv` a features/workspace/components/. src/pages/Workspace/
        ahora solo tiene index.tsx (la página/ruta). Los imports relativos entre
        ellos (`./TaskColumn`, `./types`, etc.) no cambiaron -> cero riesgo, solo
        se actualizaron los imports de index.tsx a la ruta nueva.
      [HECHO 2026-08-27] Register.tsx dividido en 3 componentes propios en
        features/auth/components/: RegisterChooseStep (buscador de empresa,
        incluye el useEffect de fotos que antes vivía en Register), JoinCompanyForm
        (Flujo A) y NewCompanyForm (Flujo B). Cada uno pasó a dueño de su propio
        estado/validación/mutación (useRegistrarEmpleado / useRegistrarEmpresa) —
        antes vivían todos juntos en el padre. Register.tsx quedó como
        orquestador fino: solo `mode` + `selectedCompanyId` (para el mensaje de
        éxito) y la pantalla de éxito. JSX copiado literal, sin tocar clases ni
        textos, para minimizar riesgo de transcripción. tsc/lint/build verdes
        (el warning de exhaustive-deps del useEffect de fotos simplemente se
        movió con el código a RegisterChooseStep.tsx, ya estaba antes).
      [HECHO 2026-08-27] Login.tsx. En vez de partirlo en "formulario" vs
        "osito" (estaban demasiado entrelazados — el oso reacciona a qué campo
        tiene foco, partirlo hubiera significado pasar ~10 props de estado de
        ida y vuelta sin ganar nada), se movió el componente COMPLETO tal cual
        a features/auth/components/LoginForm.tsx (mismo patrón que los
        Workspace/*.tsx: relocación literal, no una descomposición). pages/Login.tsx
        quedó en 4 líneas: solo renderiza <LoginForm/>. De paso se sacó el
        import muerto de `Input` (ya no se usaba, los campos son <input> nativos)
        -> warnings 33 -> 32. tsc/lint/build verdes.
  - [HECHO 2026-08-27] B7: los ~56 `any` explícitos que quedaban (AppContext,
    AdminCompanies, AdminUsers, AdminProjects, CompanyDashboard, CompanyProfile,
    MyProjects, ProjectDetail, Workspace/index.tsx y los componentes movidos a
    features/workspace/components/) se tipificaron con los tipos de dominio ya
    existentes (Project, Request, Resource, User, WorkspaceTask, WorkspaceMember,
    KanbanColumn) o con `ComponentType<{ className?: string }>` para los iconos
    de lucide-react guardados en objetos de configuración. En un par de sitios
    donde un array literal mezclaba objetos con/sin un campo opcional (p. ej.
    `pulse`/`badge` en tabs de CompanyDashboard y Workspace/index.tsx) se agregó
    el campo con valor por defecto a todos los elementos en vez de castear —
    mismo resultado, sin `any`. En dos `useDrag`/`useDrop` de react-dnd
    (TaskCard/TaskColumn) el `as any` final resultó innecesario, se quitó y
    tipa solo. Regla `@typescript-eslint/no-explicit-any` subida de "warn" a
    "error" en eslint.config.mjs — ya no se puede reintroducir `any` sin que
    falle `npm run lint`. Resultado: 90 -> 33 warnings (0 errores), tsc y build
    verdes. Los 33 que quedan son preexistentes y de otro tipo (unused-vars,
    react-hooks/exhaustive-deps, set-state-in-effect, fast-refresh) — no tocados,
    fuera de alcance de este follow-up.
  - AppContext.tsx sigue vivo; se desmonta feature por feature.


===============================================================================
FASE 3 — MANUAL UI/UX  +  ACCESIBILIDAD WCAG 2.2      ESTADO: EN CURSO (avanzada)
===============================================================================

HECHO (por slices, todos con tsc/eslint/build verdes):

  Slice 1 — Feedback + shell:
    - shared/components/feedback/: Skeleton, EstadoCargando (role=status +
      aria-live), EstadoVacio (icono+título+desc+acción), EstadoError
      (título+desc+botón Reintentar) + index.ts.  (Manual 8.9)
    - shared/components/layout/AppLayout.tsx: shell reutilizable con skip-link +
      Navbar + Sidebar + <main id="contenido" tabIndex={-1}>.
    - Skip-link global "Saltar al contenido" -> #contenido en app/providers.tsx
      (WCAG 2.4.1).
    - <main id="contenido"> en las 14 páginas con shell.
    - Navbar/Sidebar: aria-label + aria-expanded/aria-haspopup en botones de icono;
      aria-hidden en iconos decorativos; aria-current en links del sidebar;
      <nav aria-label="Navegación principal">.
    - <div onClick> -> <button> en CompanyDashboard (stat card).
    - Foco visible añadido a inputs/selects sin reemplazo (Explore, Home, Login,
      ProjectDetail).

  Slice 2 — Layout + breadcrumbs:
    - shared/components/layout/Breadcrumbs.tsx: <nav aria-label> + <ol>, última
      miga sin enlace con aria-current="page".  (Manual 3.1)
    - 10 páginas migradas a <AppLayout> (elimina el shell repetido):
      AdminCompanies, AdminCompanyRequest, AdminCompanyUsers, AdminDashboard,
      AdminProjects, AdminUsers, CompanyMembers, CompanyProfile, CreateProject,
      MyProjects.
    - <Breadcrumbs> cableado en 9 páginas internas (todas menos AdminDashboard,
      que es de 1er nivel).

  Slice 3 — Formularios (Manual 8.3 / WCAG 2.2):
    - autoComplete en TODOS los formularios de auth (email / current-password /
      new-password / one-time-code / name / organization-title / organization).
    - Login: <label htmlFor> + <input id> asociados.
    - EstadoVacio cableado en AdminCompanies, AdminUsers, AdminProjects.
    - EstadoError + botón reintentar en AdminCompanies (isError/refetch de useEmpresas).

  Slice 4 — Footer + estados:
    - shared/components/layout/Footer.tsx (Manual 3.4): <footer> con copyright de
      año dinámico + contacto (mailto) + link a Explorar. Enlaces legales NO
      puestos (las páginas no existen -> evitar 404, Manual Parte 19). Cableado en
      AppLayout (prop `sinFooter`).
    - EstadoVacio en ProjectRequests y MyProjects (tab-aware, con CTA).
    - zIndex: 9999 -> var(--z-index-popover) en el portal dropdown de MyProjects.

  Slice 5 — Modal accesible (Manual 8.6):
    - shared/components/ui/Modal.tsx: portal a body, cierra con X/ESC/clic-fondo,
      FOCUS-TRAP (Tab no sale, foco inicial al primer interactivo, devuelve el
      foco al disparador al cerrar), role="dialog" aria-modal aria-labelledby,
      bloqueo de scroll de fondo, ancho máx sm/md/lg, prop `acciones` para el pie.
    - Convertidos a <Modal>: Workspace/NewFolderModal, Workspace/TaskEditModal,
      confirm de bloqueo en AdminUsers.
    - aria-label en inputs/selects de búsqueda sin label (AdminCompanies,
      AdminCompanyUsers, AdminProjects, AdminUsers, Explore, Register).

  Slice 6 — Resto de modales:
    - TODOS los modales hechos a mano convertidos a <Modal>: CompanyMembers x2,
      CompanyDashboard, ProjectDetail x2. Total 8 modales usan el <Modal> accesible.
    - Único `fixed inset-0` restante: el click-catcher del dropdown de Explore
      (no es modal, es correcto).
    - <label> asociado a los <select> de prioridad (TaskEditModal) y nuevo
      propietario (ProjectDetail).

  Slice 7 — AppLayout + error states + área táctil:
    - Home y CompanyDashboard migrados a <AppLayout>
      (Home: sinSidebar={!currentUser}, isAdmin, sinFooter -> tiene <footer> propio,
       mainClassName="flex-1 w-full overflow-hidden").
    - isError + <EstadoError> (botón Reintentar con refetch()) en AdminProjects,
      AdminUsers, MyProjects, AdminCompanyUsers (este además pasó su empty-state
      ad-hoc a <EstadoVacio>). Ya estaba en AdminCompanies.
    - Área táctil 44px (Manual línea 890, OBLIGATORIO):
        Navbar toggle de tema + botón menú móvil  w-8 h-8 -> w-11 h-11
        Navbar botón menú de cuenta               + min-h-11
        Modal botón Cerrar                         -> min-h-11 min-w-11 (sin p-1)
        DocumentUpload / CompanyProfile "quitar"   -> min-h-11 min-w-11 + aria-label
        Register "<- Volver" x2                    + min-h-11 px-2 -ml-2
        Sidebar toggle: ya cumplía (py-3 + icono 20px).

  Slice 8 — Color sólido, sin degradados:
    - (ver detalle completo en la sección FASE 1 arriba)

  Slice 9 — Tipografía fluida + títulos de página:
    - Manual 5.2: @theme en theme.css define --text-xs..2xl con clamp() (valores
      exactos del Manual). Mínimos >= a los actuales -> nada encoge; inputs >=16px.
    - WCAG 2.4.2 / Manual 11.2 (títulos únicos y descriptivos):
        src/app/TitleSync.tsx  (useMatches + handle.titulo -> document.title
                                 = "<titulo> · ProjectHub")
        router.tsx reestructurado: ruta raíz sin path { element: <TitleSync/>,
                                   children: [...] } y handle:{titulo} en las 20 rutas.
        src/shared/utils/useDocumentTitle.ts: hook para override dinámico ->
        ProjectDetail y Workspace ponen el nombre del proyecto al cargar.


HECHO (sesión 2026-08-27, continuación):
  - Punto 3 de "FALTA": AdminCompanyRequest ahora usa useDocumentTitle(company?.nombre)
    (mismo patrón que ProjectDetail/Workspace). Título de pestaña ya no se queda
    genérico al revisar una solicitud de empresa.
  - Auditoría de área táctil 44px (Manual línea 890/973, OBLIGATORIO) sobre TODO
    src/, buscando <button>/<a> interactivos con caja fija <44px. Corregidos
    (min-h-11 min-w-11, o w-11 h-11 cuando el botón tenía espacio de sobra):
      CompanyProfile: botón cámara de foto de perfil, Ver/Descargar documento
        personal, Ver/Descargar documento de empresa (+ aria-label agregado,
        antes solo tenían `title`).
      CreateProject: quitar imagen (centrado sobre miniatura, con espacio),
        quitar recurso PDF (fila con espacio) — ambos con aria-label agregado.
      Workspace/ChatTab: botón enviar mensaje (w-9 h-9 -> min-h-11 min-w-11).
    Excepciones dejadas igual a propósito (no se agrandaron): las insignias "x"
    de esquina sobre miniaturas pequeñas en grillas (CompanyProfile galería de
    fotos, Workspace/InfoTab galería de imágenes) — son overlays hover de 24px
    en miniaturas grid-cols-3/4; agrandarlas a 44px las haría tapar la miniatura.
    Siguen cumpliendo el mínimo LEGAL de WCAG 2.5.8 (24x24px), solo no llegan al
    OBLIGATORIO del Manual (44px). Pendiente revisar en navegador si conviene
    rediseñarlas (p.ej. mostrar el botón fuera de la imagen en mobile, ya que
    group-hover no dispara con touch).
  - Auditoría estática (sin navegador) de los demás puntos del checklist 11.1-11.4:
      "Foco no oculto tras headers fijos" (2.4.11): YA CUMPLE — regla global en
        theme.css (`a, button, input, select, textarea, [tabindex] { scroll-margin-top: 6rem }`)
        cubre esto para toda la app, no solo para casos puntuales.
      "Errores identificados en texto, no solo color": YA CUMPLE — Input/TextArea
        (shared/components/ui/Input.tsx) siempre pintan icono+texto+role="alert"
        cuando hay `error`, usado en Register y CompanyProfile.
      "Orden de tabulación = orden visual en grids": revisado, no se encontró
        ningún uso de `order-*` / `flex-row-reverse` / `flex-col-reverse` que
        afecte elementos enfocables (el único flex-row-reverse es un bubble de
        chat sin controles interactivos dentro).
      "Reflow a 320px sin scroll horizontal": revisado. Los únicos elementos con
        ancho fijo >320px son: (a) fondos decorativos blur (AnimatedBackground,
        Home) dentro de contenedores `overflow-hidden`/`fixed` — no aportan al
        scroll; (b) el tablero Kanban de Workspace/TasksTab, que vive dentro de
        `overflow-x-auto` a propósito (excepción legítima de WCAG 1.4.10 para
        layouts que necesitan 2 dimensiones, tipo kanban/tabla).
    Sigue faltando la prueba real en navegador de zoom 200%/400% y espaciado de
    texto (no auditable de forma estática con confianza).

FALTA EN FASE 3 (pendiente):

  1. CONTRASTE con herramienta real (axe DevTools / Lighthouse) — Manual 11.5.
     Requiere navegador, NO se puede correr desde la terminal del agente.
     -> Tarea manual para el usuario en la PC nueva:
        - npm run dev
        - Chrome DevTools > Lighthouse > Accessibility  (en varias páginas)
        - o extensión axe DevTools > "Scan all of my page"
        - Revisar sobre todo: badges de estado con fondos -subtle, texto
          muted-foreground sobre card, botones ghost, y el modo oscuro.
     El sistema de color ya fija ratios AA en los tokens semánticos, pero falta
     la verificación empírica pantalla por pantalla.

  2. AppLayout en Explore y ProjectDetail (opcional).
     No migradas a propósito: su <div> raíz lleva clases `relative` /
     `overflow-x-hidden` que AppLayout no reproduce -> riesgo visual no testeable
     desde la terminal. Si se migran, hacerlo con `npm run dev` abierto y comparar.

  3. [HECHO 2026-08-27] Título de pestaña dinámico en AdminCompanyRequest.

  4. Auditoría fina de Manual 11.1-11.4: lo auditable de forma estática (sin
     navegador) ya se revisó y quedó documentado arriba (ver "HECHO sesión
     2026-08-27, continuación"). Queda solo lo que requiere navegador real:
     - "El texto puede ampliarse al 200% sin perder funcionalidad" (probar zoom).
     - "Espaciado de texto ajustable sin romper layout".
     - Confirmar visualmente las excepciones de 44px táctil que se dejaron
       (insignias de galería en CompanyProfile / Workspace InfoTab) — decidir si
       conviene un patrón distinto para mobile (sin hover).


===============================================================================
CÓMO CONTINUAR EN LA PC NUEVA
===============================================================================
1. git clone / pull  y  `git checkout test`.
2. cd PR-26-Busqueda-Proyectos-main  &&  npm install.
3. Confirmar base verde:  npx tsc --noEmit  &&  npm run lint  &&  npx vite build.
   (debe dar 0 errores de tsc, 0 errores de lint + 25 warnings preexistentes
   -unused-vars/exhaustive-deps/etc., no `any`-, y build compila.)
4. Retomar por prioridad — Anexo B (FASE 2) y Anexo A (FASE 1) están
   COMPLETOS en código; todo lo que queda es o (a) probar en navegador algo
   que ya se hizo, o (b) auditoría de Fase 3 que solo se puede hacer con
   navegador:
   a) Probar en navegador el slice 3/4 de Workspace/index.tsx -> react-query
      (RECURSOS+DETALLE DE PROYECTO y SOLICITUDES DE UNIÓN) — lista de pasos
      en FASE 2, sección "PENDIENTE DE VERIFICAR EN NAVEGADOR (slices 3/4 y
      4/4)". Con esto confirmado se cierra el follow-up de Anexo B al 100%.
   b) CONTRASTE con Lighthouse/axe (punto 1 de "FALTA EN FASE 3") — es lo
      único que bloquea cerrar Fase 3 formalmente.
   c) Lo que queda del checklist Manual 11.1-11.4 (punto 4: zoom 200%, espaciado
      de texto, y confirmar visualmente las excepciones de 44px táctil).
   d) Scroll del chat "un poco lento" sin terminar de resolver (no es parte de
      reglas/ en sí, es un bug de rendimiento encontrado al probar los
      slices — ver FASE 2, sección CHAT, punto 6, para retomarlo con más
      precisión del usuario sobre cuándo pasa exactamente).
5. Trabajar en slices pequeños y verificables; commitear cada slice.
6. El detalle fino y el historial de decisiones está también en la memoria del
   agente (archivo reglas-carpeta-estandar-vinculante.md del proyecto).
===============================================================================
